interface:
  display_name: "CC3 PG Reverse And TS Integration"
  short_description: "Convert PG Cocos JS to bound Cocos 3 TypeScript."
  default_prompt: "Use $cc3-res-reverse on this {gameId}_UI PG Cocos project, preserve every Prefab and its meta, normalize resources to {gameId}_res, convert through TS24 and TS3, apply the high-risk NumberDisplay* and numeric-atlas checklist, then verify every serialized script binding."
