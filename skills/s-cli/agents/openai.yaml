interface:
  display_name: "s_cli"
  short_description: "以目標專案本地競品資源與歸檔腳本執行 Slot 客戶端工作流"
  default_prompt: "Use $s-cli to analyze this Slot client request from local references. If doc/project_info.md is missing, ask only for the absolute HTTP(S) competitor URL, initialize the file once, then continue without external-project evidence."
