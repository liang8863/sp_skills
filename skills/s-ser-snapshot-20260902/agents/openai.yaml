interface:
  display_name: "s_ser snapshot 2026-09-02"
  short_description: "Archived Slot server workflow snapshot"
  default_prompt: "Use the current $s-ser skill for live server work; this snapshot is historical only."

policy:
  allow_implicit_invocation: false
